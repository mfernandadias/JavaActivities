package intermediarioRevisao.enumeration;

public class NivelTeste {
    public static void main(String[] args){

        /*
        Transforme uma String em enum usando valueOf().
        Exemplo: "Bom" -> Nota.BOM
        criar o enum Nota com valores como RUIM, REGULAR, BOM, etc
        No main, transformar uma String(por exemplo, "Bom") em um
        valor do enum usando valueOf()
         */
        //exemplo de entrada em string
        String entrada = "Bomm";

        /*

         */
    }
}
